import React from 'react'
import ReactDOM from 'react-dom/client'
import AlfredCommandCenter from './alfred-command-center.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AlfredCommandCenter />
  </React.StrictMode>,
)
